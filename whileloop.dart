// executes  a condition until its false
void main() {
  var a = 1;
  var maxnum = 10;
  while (a < maxnum) {
    print(a); // prints untils its false
    a = a + 1;// increases value of a after every iteration/loop/cycle
  }
}
